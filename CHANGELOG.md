# 更新日志

本文件记录 DSH-manager 的版本更新进度。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/),
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。应用内显示的版本号取自 `main.py` 的 `APP_VERSION`,发版时与 git tag 同步更新。

## [1.1.0] - 2026-09-06

### 新增
- **dsh web 认证适配**:新版 DSH 的 `dsh web` 带浏览器会话认证(进程级 token),管理器现在从后端输出解析带 token 的 URL,「打开前端」与就绪日志均使用认证地址,不再出现 401 "dsh web authentication required";旧版无认证时自动回退裸地址
- **工作区迁移预检**:切换路径前先全量预检(不动文件、不停后端)——新路径嵌套在旧路径内直接拒绝;新路径已存在的同名完整源码自动跳过并视为已迁移(支持"先手动复制再切换"的工作流);残缺副本逐个弹窗询问(删除并移动/跳过/取消),程序绝不擅自删用户手动放置的文件
- **工作区迁移进度反馈**:迁移期间弹出模态进度弹窗,逐版本显示"正在迁移 x/y 份源码:目录名";失败清理半成品时同样显示"正在清理残留 x/y",不再有假死观感
- **单实例锁**:重复启动时不再开第二个窗口,而是激活已运行的实例
- **后端日志落盘**:DSH 后端输出写入日志文件,便于排查问题
- **端口冲突自动换端口**:默认端口被占用时自动顺延,并同步通知前端
- 主界面标题栏显示应用版本号(取自 `main.py` 的 `APP_VERSION`)

### 修复
- **工作区迁移数据安全**:后端运行中切换路径时先自动停止后端再动文件(此前 node 进程锁定 `node_modules` 句柄导致整目录改名失败、退化为逐文件复制、中途抛错留下半成品);失败清理只删除本次尝试复制出的目录,绝不动动手前已存在的目标目录(此前会把用户预先复制到新路径的完整副本当成半成品删光);只有全部移走才算成功,失败则配置不切换
- **后端日志文件生命周期**:启动流程返回后日志文件即关闭,而后台 pump 线程仍在输出——后续日志(含 token URL)会令 `log_file.write` 抛 ValueError 弄死 pump 线程,日志落盘只剩前几行;现在容忍已关闭的文件句柄
- pnpm 11 运行前依赖检查在无 TTY 环境下 abort 导致后端启动失败 —— 所有 pnpm 子进程注入 `pnpm_config_verify_deps_before_run=false` 跳过检查

### 变更
- **日志区操作可发现性优化**：「清空」与「打开日志目录」由灰色纯文字链接升级为小型描边按钮(常显),文案「日志目录」改为「打开日志目录」并补充明确悬浮提示;顺带修正日志头部布局(标题靠左、按钮成组靠右)
- 程序名统一为 **DSH-manager**:exe 文件名、窗口标题、界面标题、Release 附件全部去中文名
- **原生标题栏按应用主题染色**(Windows 11 DWM `DWMWA_CAPTION_COLOR`):深色主题染深蓝、浅色主题染浅灰蓝,随系统深浅色实时切换,旧 Win10 自动回退沉浸式深色模式
- **顶栏去重**:应用内顶栏不再重复显示 "DSH-manager"(由原生标题栏承担),只保留 logo 与 "DeepSeek Harness" 副标
- 日志区字体由等宽字体改为微软雅黑,与全局一致

## [1.0] - 2026-08-23

首个发布版本。

### 新增
- **核心流程**:版本查询 → 源码下载 → 后台运行 → 打开前端 → 停止
- **发行版本**:从 GitHub 拉取 `deepseek-ai/deepseek-harness` 的 tag 列表并按版本号排序,支持强制刷新
- **源码下载**:浅克隆(`git clone --depth 1 --branch <tag>`)指定版本到 `data/repos/`,支持断点复用与重复下载判定
- **后台运行**:启动 / 停止 / 删除已下载源码;后端就绪探测、智能跳过 install/build
- **工作区管理**:路径设置、目录浏览(原生对话框)、切换前预览与应用、旧工作区移动/删除/保留
- **代理设置**:查版本、下源码可走自定义代理
- **主题**:界面跟随系统深浅色并实时切换
- **应用图标**:exe 图标、窗口标题栏图标、界面左上角 logo 统一
- **自动发布**:GitHub Actions 构建并附带到 Release

### 修复
- 关闭窗口自动停止 DSH 后端;启动前清理端口残留进程,避免孤儿进程占端口
- pywebview 桥就绪时序问题:不再开局误判为普通浏览器,修复点击/刷新/设置不可用
- 后端日志改为 `evaluate_js` 实时推送,替代不可用的 JS 回调桥
- 子进程隐藏控制台黑窗;目录选择改走原生对话框
